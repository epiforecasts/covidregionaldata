on:
  schedule:
    - cron: '36 12 * * *'
  workflow_dispatch:

name: Google

jobs:
  Google:
    uses: ./.github/workflows/test-regional-datasets.yml
    with: 
      test-source: ${{ github.workflow }}
